---
import CmsPage from "../components/CmsPage.astro";
import { getPublicPageBySlug } from "../lib/cms";

const page = await getPublicPageBySlug("sede");
if (!page) throw new Error("Missing CMS page: sede");
---

<CmsPage page={page} />
