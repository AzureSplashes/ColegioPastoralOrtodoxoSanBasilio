---
import CmsPage from "../components/CmsPage.astro";
import { getPublicPageBySlug } from "../lib/cms";

const page = await getPublicPageBySlug("contacto");
if (!page) throw new Error("Missing CMS page: contacto");
---

<CmsPage page={page} />
