---
import CmsPage from "../components/CmsPage.astro";
import { getPublicPageBySlug } from "../lib/cms";

const page = await getPublicPageBySlug("politica-de-privacidad");
if (!page) throw new Error("Missing CMS page: politica-de-privacidad");
---

<CmsPage page={page} />
