---
import CmsPage from "../components/CmsPage.astro";
import { getHomepage } from "../lib/cms";

const page = await getHomepage();
if (!page) throw new Error("Missing CMS page: inicio");
---

<CmsPage page={page} />
