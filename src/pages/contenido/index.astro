---
import CmsPage from "../../components/CmsPage.astro";
import { getPublicPageBySlug } from "../../lib/cms";

const page = await getPublicPageBySlug("contenido");
if (!page) throw new Error("Missing CMS page: contenido");
---

<CmsPage page={page} />
